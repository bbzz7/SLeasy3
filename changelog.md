# 更新记录

本文根据 `build/SLeasy3.js` 顶部注释整理。版本顺序与原记录一致；未标注日期的早期版本记为“日期未记录”。

## 3.9.23（2024-11-17）
- `SLeasy.insert()` 支持递归。

## 3.9.22（2024-06-03）
- 更新默认背景音乐及音乐按钮，支持 Web Audio；添加 `SLeasy.initMedias()`。

## 3.9.21（2023-12-13）
- 添加分组系统，`subMotion` 自动递归；`SLeasy.path()` 自动获取 `app.js` 的时间戳。

## 3.9.20（2023-12-05）
- 更新 Gulpfile，新增 `deploy`、`deploy-noMin` 生产部署任务；集成 VConsole；添加 `SLeasy.goPre()`。

## 3.9.19（2023-11-26）
- 添加 `floatZIndex`、`detailZIndex` 配置参数。
- 添加 `$scope.loadingTotalTime`、`SLeasy.isWmp()`、`SLeasy.isLocalHost()`、`SLeasy.getURLParams()`。
- 完善 Scroll 模式下的水平滑动场景；修复自定义 Loading 自动跳转首页的问题。
- 优化 `SLeasy.insert()`，使其支持 `set` 中的所有属性。

## 3.9.18（2023-06-14）
- 重构 `SLeasy.music` 初始化逻辑；新增 `SLeasy.spriteNext()`、`jssdk.previewImage()`；为 `img` 元素添加 `content` 选项；修复若干问题。

## 3.9.17（2022-07-25）
- 新增 `SLeasy.percent()`、`SLeasy.userSelect()`；添加 `hold`、`holdup` 事件；进行若干优化。

## 3.9.16（2021-11-25）
- JSSDK 添加关闭当前网页窗口 API；更新 iOS 摇一摇授权逻辑；`input` 添加 `password` 参数；更新所有幻灯均无子元素时跳转首页的逻辑。

## 3.9.15（2021-10-28）
- 首页改为在所有图片加载完毕及 `SLeasy.init().done()` 后出现；添加 `regions` 全国三级省区 JSON 数据。

## 3.9.14（2021-10-14）
- 添加 `SLeasy.keepLastIndex()`；更新 `SLeasy.viewScale()`、`SLeasy.insert()`。

## 3.9.13（2021-05-25）
- 修复幻灯子元素在 Safari 中执行 3D 变换时的显示问题；新增 `x`、`y` 自适应偏移条件；添加 `SLeasy.pauseMedia()`、`SLeasy.music.volume()`。

## 3.9.12（2021-04-27）
- 增强 `SLeasy.respY()` 自适应功能，允许详情页多层叠加。

## 3.9.11（2020-12-01）
- 增加横竖屏旋转模式 `config.rotateMode`。

## 3.9.10（2020-09-12）
- 增加 `fixWidthMode` 自适应宽度模式开关；完善 `stageMode` 阈值模式。

## 3.9.9（2020-09-06）
- 将 `fixHeight` 加 1，避免小数造成底部背景缝隙；增加 `SLeasy.respY()`，解决元素超出上下安全区域时的自适应定位问题。

## 3.9.8（2020-07-26）
- 修复 `x`、`y` 自适应偏移判断问题；将配置函数值计算结果回滚为原生值；添加 SortableJS。

## 3.9.7（2020-05-20）
- 重构 `subMotion` 时间轴起始时间点定位。

## 3.9.6（2020-05-17）
- 为自定义 Loading 和 Slider 添加 `on` 事件绑定参数选项。

## 3.9.5（2020-05-12）
- 完善 Height 模式下的元素自适应。

## 3.9.4（2020-05-04）
- 优化资源预加载，使图片（包括 AE 类型）完全按配置顺序加载；Web Audio 模式下内置音乐按钮增加全局静音功能。

## 3.9.3（2020-04-26）
- 完善 ScrollMagic 模式。

## 3.9.2（2020-04-22）
- 参考 `ios-inner-height.js`，内置获取全面屏 iPhone 在钉钉、手淘中全屏高度的兼容方法。

## 3.9.1（2020-04-22）
- 添加 `iosInnerHeight()`，解决全面屏 iPhone 在钉钉、手淘中的全屏高度兼容问题。

## 3.9.0（2020-04-22）
- 添加 ScrollMagic 模式。

## 3.8.23（2020-03-18）
- `SLeasy.resize()` 支持适配 X5 同层模式；Video 元素的 `object-fit` 默认值改为 `cover`。

## 3.8.22（2020-01-19）
- 更新、添加并修复若干内容。

## 3.8.21（2020-01-16）
- 优化幻灯子元素起始时间为 0 时的场景和边界判断，不再等待页面切换时间。

## 3.8.20（2019-10-31）
- 新增 `letter-spacing` 属性缩放变换；单独提取 `SLeasy.bitConvent()`。

## 3.8.19（2019-08-31）
- 新增 `SLeasy.copyText()`；修正 Video 元素的 `controls` 属性；幻灯页新增 `preMotionFX` 选项。

## 3.8.18（2019-07-03）
- 新增 `SLeasy.loadAeLayer()`，支持按需加载。

## 3.8.17（2019-07-02）
- 调整资源加载顺序，将 AE 序列帧资源置于最后。

## 3.8.16（2019-05-29）
- 支持指定 N 个线程下载；进一步完善 `SLeasy.playAeLayer()`、`SLeasy.show()`、`SLeasy.hide()` 等函数。

## 3.8.15（2019-04-17）
- 初步集成 Web Audio 和 Howler。

## 3.8.14（2019-04-14）
- 进一步增强 Sprite 元素及 Media 系列函数。

## 3.8.13（2019-04-09）
- 添加 Sprite 元素及相关函数。

## 3.8.12（2019-04-03）
- 添加 `SLeasy.initMedia()`、`SLeasy.media()`、`SLeasy.pauseAeLayer()`、`SLeasy.resumeAeLayer()`；修复 `SLeasy.playAeLayer()` 接收数组对象参数时偶发跳帧的问题。

## 3.8.11（2019-03-31）
- 自定义 Loading 添加 `onComplete` 钩子；修复 `subMotion` 中 `addPause()` 时间定位不准确的问题。

## 3.8.10（2019-03-02）
- 完整支持 `subMotion.to`；幻灯页可分别设置入场、出场效果；支持自动缩放 `+=`、`-=` 相对值。

## 3.8.9（2019-02-22）
- 内置 AE 插件支持合并同一 AE Layer 的多种格式序列，并支持同时传递 AE Layer 播放对象数组。

## 3.8.8（2019-02-20）
- 完善内置 AE 插件的 Img 渲染引擎，支持多层 AE Layer。

## 3.8.7（2019-02-17）
- 完善自定义 Loading 模块；移除 `noFade`、`autoStart` 参数的相关控制逻辑。

## 3.8.6（2019-01-29）
- 修复 `top` 值计算错误。

## 3.8.5（2019-01-23）
- 内置 AE 插件增加 `SLeasy.gotoAeLayer()`。

## 3.8.3（2019-01-21）
- 添加文件时间戳，用于刷新 CDN 缓存。

## 3.8.2（2019-01-16）
- 修复 SLeasy 初始化回调中元素 `imgToDiv` 计数错误的问题。

## 3.8.1（2019-01-08）
- 修复启用自定义 Loading 时，二次初始化导致最外层 `SLeasy.init().done()` 回调失效的问题。

## 3.8.0（2019-01-07）
- 内置 AE 插件重大升级，重构为 Easel、Pixi、Img 三种渲染引擎。

## 3.7.7（2019-01-06）
- 更新 `SLeasy.init().done()` 的执行逻辑。

## 3.7.6（2019-01-05）
- 修复 `musicBt` 图片地址未经过 `SLeasy.path()` 处理的问题；更新 `SLeasy.isHttp()`、`SLeasy.path()`。

## 3.7.5（2018-12-30）
- 重构内置 AE 插件，提升性能。

## 3.7.4（2018-12-29）
- 解决 iOS 微信中横竖屏切换导致布局尺寸异常且无法复位的问题。

## 3.7.3（2018-12-13）
- 支持为单页幻灯设置 `alignMode` 参数。

## 3.7.2（2018-06-21）
- 大幅优化 AE 插件资源释放；支持自定义 Loading 自动播放 AE 序列；添加横竖屏旋转回调等功能。

## 3.7.1（2018-06-19）
- 优化 Debug 模式和 `force3D` 全局配置；修复 `alignMode` 相关定位问题等。

## 3.7.0（2017-07-19）
- 更新高级自定义 Loading 功能模块。

## 3.6.0（2017-06-19）
- 更新依赖库、第三方库及 Gulp 构建工具链。

## 3.5.6（2017-01-06）
- 添加 Android 微信 H5 同层播放器相关代码；Viewport 添加 `device-width` 模式。

## 3.5.5（2016-12-05）
- 添加幻灯页 Scroll 模式；修复子动画时间为 0 时开始时间定位错误的问题。

## 3.5.0（2016-12-03）
- 将子动画时间轴开始时间重构为绝对时间值；添加 `loaderMsg` 模块。

## 3.4.0（2016-09-25）
- 将子动画队列播放重构为与场景切换动效前置对齐的模式；添加 Audio、Video、Iframe 等内置元素。

## 3.3.1（2016-08-14）
- 添加传统 Click 事件支持，以兼容部分广告监测代码；`shadownBt` 添加 `name` 属性；Debug 模式添加对比阴影。

## 3.3.0（2016-04-17）
- 修复 `backfaceVisiblity` 问题；添加阈值高度适配模式；优化 Gulpfile 自动化构建。

## 3.2.7（2015-12-23）
- 修复 `SLeasy.cache()`；添加更多 `fixProps`。

## 3.2.6（2015-08-20）
- 修复幻灯切换效果索引不为 0 时详情页默认切换效果获取错误的问题；优化 Loading 结构。

## 3.2.5（2015-08-18）
- 调整模块目录；增加 Mask 涂抹遮罩插件；修正幻灯切换状态锁定；优化 Gulpfile 自动化构建。

## 3.2.0（2015-07-22）
- 加强路由功能；解耦并重构动画模块；增加打开淘宝、天猫客户端的跳转功能。

## 3.1.0（2015-06-28）
- 引入全新路由功能，支持所有切换及详情页开关状态。

## 3.0.0（日期未记录）
- 全新架构，重大升级。

## 2.8.7（日期未记录）
- 兼容微信背景音乐自动播放及视频行内播放。

## 2.8.5（日期未记录）
- 增加子动画 `to` 配置参数，支持同一元素连动。

## 2.8（日期未记录）
- 大幅升级并重构内部初始化流程。
- 增加资源预加载模块，支持可配置的内置 Loading 样式。
- 增加多线程异步乱序加载和单线程顺序加载两种模式。
- 增加固定浮动元素、加载回调及幻灯切换回调，并改善显示体验。
